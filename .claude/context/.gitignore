# Context Directory - Runtime Data
# Keep structure, ignore runtime data

# Session state (regenerated on each run)
session.json
*.log
cleanup-log.json

# Runtime directory (all runtime data)
runtime/**/*

# Generated artifacts (user-generated, may be large)
artifacts/generated/**/*

# Required reference artifacts at root (for backward compatibility with workflows)
!artifacts/standard-plan-rubric.json

# Reasoning history (can be regenerated)
history/**/*.json
history/**/*.md

# Quality gates (ephemeral)
history/gates/**/*.json

# Audit logs (can grow large)
audit/**/*.json
audit/**/*.log

# Memory + performance (runtime)
memory/**/*
performance/**/*

# Keep directory structure
!.gitkeep
!artifacts/.gitkeep
!artifacts/reference/.gitkeep
!history/reasoning/.gitkeep
!history/gates/.gitkeep
!audit/.gitkeep
!runtime/.gitkeep

# Config directory (should be committed)
!config/
config/*.json
!config/rule-index.json
!config/cuj-registry.json
!config/skill-integration-matrix.json
!config/plan-review-matrix.json
!config/signoff-matrix.json
!config/security-triggers-v2.json

# Reference artifacts (should be committed)
!artifacts/reference/
!artifacts/reference/**/*

# Legacy paths (for backward compatibility during migration)
# These will be removed after full migration
!rule-index.json
!cuj-registry.json
!skill-integration-matrix.json
!plan-review-matrix.json
!signoff-matrix.json
!security-triggers-v2.json
