---
description: List or run on-disk instruments (Agent Zero–style helpers).
---

Use `Bash` to run:

- `node .claude/tools/instruments.mjs list`

If the user asks to run one, execute:

- `node .claude/tools/instruments.mjs run <name> -- <args...>`
