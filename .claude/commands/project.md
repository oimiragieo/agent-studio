---
description: Set or inspect the active project namespace (isolates runtime runs/jobs inside this repo).
---

Use `Bash` to run:

- Current: `node .claude/tools/project.mjs current`
- Use: `node .claude/tools/project.mjs use "<name>"`
- Clear: `node .claude/tools/project.mjs clear`
